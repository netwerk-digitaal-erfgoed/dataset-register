SPARQL_URL=http://localhost:7001
SPARQL_ACCESS_TOKEN=SECRET_TOKEN

# Local Typesense (the `docker compose` service). With these set, the crawler
# rebuilds the search index after each crawl; production overrides them.
TYPESENSE_HOST=localhost
TYPESENSE_API_KEY=dev-typesense-key
